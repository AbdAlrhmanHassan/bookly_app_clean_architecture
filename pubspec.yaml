name: bookly_app_advanced_course
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.0-213.0.dev <4.0.0'

dependencies:
  dartz: ^0.10.1
  dio: ^4.0.6

  flutter:
    sdk: flutter
  font_awesome_flutter: ^10.5.0
  go_router: ^10.0.0
  google_fonts: ^5.1.0

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/
